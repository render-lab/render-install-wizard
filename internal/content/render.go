package content

// Render converts markdown into terminal-styled output. It currently returns the
// input unchanged.
func Render(markdown string) string {
	// TODO(phase 1E): apply TUI styling to the markdown.
	return markdown
}
